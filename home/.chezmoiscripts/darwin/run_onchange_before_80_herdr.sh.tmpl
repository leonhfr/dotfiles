#!/bin/bash
set -euo pipefail

plugins=(
    "thanhdat77/herdr-navigator"
)

if command -v herdr >/dev/null 2>&1; then
    echo "[dotfiles] Installing herdr plugins"
    for plugin in "${plugins[@]}"; do
        herdr plugin install "$plugin" --yes
    done
fi
