#!/bin/bash
set -euo pipefail

eval "$(/opt/homebrew/bin/brew shellenv)"

echo "[dotfiles] Syncing repos"

{{- $repos := concat .repos.common (index .repos .machine) }}
printf '%s\n' \
{{- range $repos }}
  {{ . | squote }} \
{{- end }}
| ghq get --parallel
